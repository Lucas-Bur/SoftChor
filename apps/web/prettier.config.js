/**
 * Web app Prettier configuration
 * Extends the shared Prettier config from @repo/prettier-config
 */

import baseConfig from '@repo/prettier-config'

/** @type {import('prettier').Config} */
const config = {
  ...baseConfig,
}

export default config
